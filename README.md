啊♂乖乖站好
===================

更改 文件夹 内的所有 文件名字 为 **随机生成的hash值** 或者 按**数字顺序排列**的名字。
 
~~（神TM的随机生成）~~

只是方便使用本地图片测试而写的。

###使用
```
var test = require('./changeName.js');
var path = './omg/';
test(path, 'hash');
```